// Prevent us from attempting to use variables
// that are not declared
/*
"use strict"

// Create arrays
let numbersArray = [1, 2, 3, 4, 5]; // Array with numbers
let mixedArray = [42, "hello", true, null, { key: "value" }, [1, 2, 3]]; // Array with different data types

// Log arrays to the console
console.log("Numbers Array:", numbersArray);
console.log("Mixed Array:", mixedArray);

// Log the typeof one array
console.log("Type of numbersArray:", typeof numbersArray); // Returns "object"

// Remove an item from each array
numbersArray.pop(); // Removes the last element (5)
mixedArray.shift(); // Removes the first element (42)

// Log arrays after removing items
console.log("Numbers Array after removing item:", numbersArray);
console.log("Mixed Array after removing item:", mixedArray);

// Add an item to each array
numbersArray.push(6); // Adds 6 to the end of the array
mixedArray.unshift("new element"); // Adds "new element" to the start of the array

// Log arrays after adding items
console.log("Numbers Array after adding item:", numbersArray);
console.log("Mixed Array after adding item:", mixedArray);

// Change the value of an element in each array
numbersArray[0] = 10; // Changes the first element to 10
mixedArray[2] = false; // Changes the third element to false

// Log arrays after changing values
console.log("Numbers Array after changing a value:", numbersArray);
console.log("Mixed Array after changing a value:", mixedArray);
*/